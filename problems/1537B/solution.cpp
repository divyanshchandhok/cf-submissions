#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve(){
    int n, m, i, j;
    cin >> n >> m >> i >> j;

    cout << "1" << " 1" << " " << n << " " << m << endl;
}

signed main(){
    cin.tie(0);
    ios::sync_with_stdio(0);

    int t;
    cin >> t;
    while(t--){
        solve();
    }
}