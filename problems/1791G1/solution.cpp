#include <bits/stdc++.h>
#define int long long

using namespace std;

void solve(){
    int n, c;
    cin >> n >> c;

    int a[n];

    for(int i = 0; i < n; i++){
        cin >> a[i];
    }

    cout << rec(0, )
}

signed main(){
    int t;
    cin >> t;
    while(t--){
        solve();
    }
}